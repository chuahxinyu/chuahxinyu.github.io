---
const {frontmatter} = Astro.props;
---
<html>
  <head>
      <title>{frontmatter.title}</title>
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.4/dist/katex.min.css" crossorigin="anonymous">
  </head>
  <body class="flex min-h-screen flex-col">
    <div class="container prose m-auto flex max-w-[80vw] flex-grow flex-col xl:max-w-screen-xl">
      <button class="btn btn-primary">Get Started</button>
      <h1 class="text-5xl font-bold">{frontmatter.title}</h1>
      <slot />
    </div>
  </body>
</html>